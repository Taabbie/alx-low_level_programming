#include "main.h"

/**
 * mul - multipying two numbers
 * @a: first integer to be multiplied to b
 * @b: second integer to be multiplied to a
 * Return: the result of the operation
 */
int mul(int a, int b);
{
	int c;

	c = a * b;

	return (c);
}
